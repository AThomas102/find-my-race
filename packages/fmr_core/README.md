# fmr-core

Pure Python package: race/field models, equivalent performance conversion, and search scoring.

Install editable from repo root:

```bash
pip install -e ./packages/fmr_core
```

Run tests:

```bash
cd packages/fmr_core && pip install -e ".[dev]" && pytest
```
